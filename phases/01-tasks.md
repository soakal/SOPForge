# Phase 1 — Task list

Rationale: UIA element resolution across Win32/Chromium/Electron is the phase's stated
kill-risk, so the spike lands immediately after deps exist; everything else builds
outward from manifest schema (ground truth) to modules to harness, with packaging and
measurement last. Redaction OCR is the second unknown, so it precedes tray/orchestration
polish; all verify commands run from `C:\Users\Brian\Documents\SOPForge` with the repo venv.

- [ ] task-01: Pin and install Phase 1 deps into `requirements.txt` (mss, pynput, pywin32, comtypes, pywinauto, Pillow, numpy, pystray, pyinstaller) plus `pyproject.toml` with src-layout `capture` package installed editable (`.venv\Scripts\pip.exe install -e .`) — verify: `.venv\Scripts\python.exe -c "import mss, pynput, comtypes, win32api, pywinauto, pystray, PIL, numpy, PyInstaller, capture; print('ok')"`
- [ ] task-02: UIA spike `scripts/spike_uia.py` — launch Notepad, Chrome, and VS Code via pywinauto, resolve the UIA element at a known click point in each (element-from-point, name/control_type/automation_id/framework/bounding_rect), print per-app JSON, exit 0 only if all three apps return non-empty `name` or `control_type` AND findings (Chrome accessibility activation, Electron quirks, timing) were appended to `.claude/skills/uia-notes.md` — verify: `.venv\Scripts\python.exe scripts\spike_uia.py`
- [ ] task-03: Author `fixtures/manifest.schema.json` (JSON Schema draft 2020-12) matching `fixtures/sample-manifest.json` shape incl. click/type variants, empty-element records, and redactions array; test asserts sample validates and three mutated-invalid variants fail — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_manifest_schema.py`
- [ ] task-04: `src/capture/manifest.py` — session builder, ordered step append with `step-NNN` IDs, atomic write; tests cover round-trip schema validation, order preservation, and the all-empty-elements worst case (criterion 5), emitting `fixtures/empty-elements-manifest.json` as a Phase 2 fixture — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_manifest_writer.py`
- [ ] task-05: `src/capture/uia.py` — hardened resolver from spike learnings: element-from-point with per-call timeout, framework/window-class detection (win32/chromium/electron), guaranteed empty-metadata dict on failure (never raises); interactive test resolves a live Notepad control and asserts empty-safe path on an off-screen point — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_uia_resolver.py`
- [ ] task-06: `src/capture/hooks.py` (pynput click/type listener with type-content-not-captured summarization) + `src/capture/shots.py` (mss screenshot-on-event, monitor mapping, `NNN.png` naming); test injects synthetic clicks via pynput Controller and asserts PNGs exist with correct numbering and event records carry screen coords — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_hooks_shots.py`
- [ ] task-07: Redaction engine `src/capture/redact.py` + `config/redaction.toml` (email, IPv4, password-field heuristic regexes) using Windows OCR (winsdk, pinned) with UIA-text fallback; tests seed PIL-rendered images containing known email/IP strings and pixel-diff assert the matched regions are blurred and non-matched regions untouched (criterion 3) — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_redaction.py`
- [ ] task-08: Optional narration `src/capture/narration.py` — WAV record start/stop via pywin32/winmm, writes `narration.wav` and sets manifest field; no-microphone path degrades to `narration_wav: null` without error; test asserts valid RIFF header when a device exists else asserts the clean-skip path — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_narration.py`
- [ ] task-09: Recorder orchestration `src/capture/recorder.py` — session lifecycle wiring hooks→shots→uia→manifest→redaction into `captures/<session-id>/`; test drives a scripted synthetic-click session and asserts the output directory contains numbered PNGs plus a schema-valid manifest whose step order matches injection order — verify: `.venv\Scripts\python.exe -m pytest -x -q tests/test_recorder.py`
- [ ] task-10: Tray app `src/capture/tray.py` + `src/capture/__main__.py` — pystray icon with recording indicator, start/stop hotkey, Exit menu; `--self-check` flag initializes tray+hotkey+recorder, then exits 0 without user input — verify: `.venv\Scripts\python.exe -m capture --self-check`
- [ ] task-11: Self-test harness `src/capture/selftest.py` — pywinauto drives Notepad, Chrome, VS Code through fixed click sequences against a live recorder; asserts manifest schema-valid, step order matches script order, ≥90% non-empty element metadata overall; writes per-app breakdown to `phases/01-results.md` (criteria 1–2); exit nonzero below threshold — verify: `.venv\Scripts\python.exe -m capture.selftest --all`
- [ ] task-12: Elevated-focus hotkey check `scripts/check_elevated_hotkey.py` — launches an elevated window, exercises the hotkey, exits 0 if start/stop fired OR `phases/DEVIATIONS.md` contains a `## UIPI` section documenting the limitation (criterion 6) — verify: `.venv\Scripts\python.exe scripts\check_elevated_hotkey.py`
- [ ] task-13: `sopforge.spec` PyInstaller build — onefile, `console=False`, excludes trimmed for <40MB; build script asserts `dist/sopforge.exe` exists and size <40MB — verify: `.venv\Scripts\python.exe scripts\build_exe.py --assert-size 40`
- [ ] task-14: EXE smoke + timing `scripts/verify_exe.py` — cold-launch `dist/sopforge.exe`, poll for tray icon presence, assert appearance <2s and clean exit via tray Exit message, record measured number into `phases/01-results.md` (criterion 4) — verify: `.venv\Scripts\python.exe scripts\verify_exe.py`
- [ ] task-15: Close out Phase 1 — `phases/01-results.md` records evidence for all six acceptance criteria; full gate green — verify: `.venv\Scripts\ruff.exe check src/ && .venv\Scripts\ruff.exe format --check src/ && .venv\Scripts\python.exe -m pytest -x -q && .venv\Scripts\python.exe -c "import pathlib,sys; t=pathlib.Path('phases/01-results.md').read_text(); sys.exit(0 if all(f'Criterion {i}' in t for i in range(1,7)) else 1)"`
